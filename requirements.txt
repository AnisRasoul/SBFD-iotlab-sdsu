tensorflow>=2.13,<2.16
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
imbalanced-learn>=0.11
# Optional model-export packages (used in Step 10 of train.py; imports are
# wrapped in try/except so the script still runs if these cannot be installed)
micromlgen>=1.1.0
m2cgen>=0.10.0
emlearn>=0.19.0
